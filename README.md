## CRUD операции с базой данных через PDO

Продолжаем работу crud-проектом  
Работа с git (предварительная работа)  
```
запустите Git Bash 
перейдите в каталог c:/xampp/htdocs/crud
> cd c:/xampp/htdocs/crud

Запуште изменения с предыдущего занятия, если вчера этого не сделали
Получите обновления с сервера
git pull
cоздайте новую ветку:
git checkout -b task-08-04-26

Запустите XAMP + Apache + mySQL
```

Основное задание  
используйте документацию по [PDO](https://www.php.net/manual/ru/book.pdo.php)  
видео по теме [PHP + PDO. Работа с MySQL](https://www.youtube.com/watch?v=a9l3QPMqZ1Q)  
```
Создайте в таблице еще один столбец с кнопками-ссылками вида
Редактировать - $html .= '<a href="?action=edit&id=' . $row["id"] . '" class="btn btn-sm">Редактировать</a> ';
Удалить - $html .= '<a href="?action=delete&id=' . $row["id"] . '" class="btn btn-sm" onclick="return confirm(\'Удалить?\')">Удалить</a>';

В конце таблицы добавьте кнопку "Добавить запись"
Добавить запись - $html .= '<a href="?action=insert" class="btn btn-primary mb-3">Добавить запись</a>

В контроллере RecordController вызывайте методы модели Record для изменения, добавления, удаления записи
в зависимости от значения GET-параметра $_GET['action']

Для добавления и изменения записи используйте вызов и обработку формы ввода данных.
Вместо физического удаления (в базе данных) сделайте пометку на удаление (добавьте поле is_deleted int 0-default).
```
Добейтесь, чтобы все CRUD-операции работали.

Задание со звездочкой (*) - Пагинация
```
Если вы сами, или другой искусственный или естественный разум, уже реализовали основное задание.
Тогда добавьте еще 20 записей в таблицу базы данных и реализуйте механизм пагинации - разбиения на страницы по 10 записей
с отображением иконок перехода к следующей, предыдущей, первой, последней страницам и по номерам
```

Закоммитьте и запуште изменения
```
> git status
> git add .
> git status
> git commit -m "Crud продолжение"
> git push
```
Сдайте работу - создав запрос на изменения Pull Request  
- зайдите на github и создайте Pull Request со своего аккаунта в исходный репозиторий (для аккаунта Coopteh)

